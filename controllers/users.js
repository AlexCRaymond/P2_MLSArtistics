const express = require('express');
const router = express.Router(); // what we attach routes too
const User = require ('../models/users.js')
const Photo = require ('../models/photos.js')

module.exports = router;